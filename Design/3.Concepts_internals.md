# Concepts internals

## package opsHub
Logic: 
  . The package `opHub` separates and mediates all operations between webserver side and Models   
  . This way we could centralize and control all operations required by webserver, in a separate way from the Models.  
  . And in this opHub it would be possible to enforce user-authz :   
    + All types zzzReq should contain a field `Client CurrentClient` that will be used to validate access in the methods  
    + All methods should validate access of `Client CurrentClient`, and return an error if access is denied  
  . Webserver package should call OpHub methods, which in turn should call Models methods


## package models and dbModels 

```

  Collection
    + has 1 AccessPolicy (1Collection-to-1AccessPolicy)
    + has zero-or-more ColRevision(s) (1Collection-to-manyColRevision)
    . Logic:
      . When new collection is created, it creates an initial ColRevision with RevState "Ready".
        So Col.ColRevisions[] is never empty and Col.GetColRevisionLatest always returns a valid ColRevision
      . When user initiates a CollectionEdit, then Collection creates a new ColRevision (with initialRevState "CollectionEditOngoing")
      . Webages can check at any moment if a CollectionEdit is "enabled" to start, by calling Collection.IsEditable()==true 


  ColRevision
    + has one-or-more RevState(s) (1ColRevision-to-manyRevState)

  RevState
   
```




### AccessPolicy: DONE, see godocs
+ 2 hardcoded roles: admin, reader 
+ adminUsers, adminGroups, readerUsers, readerGroups


### Collection
+ **CollectionNew(...)**
+ **CollectionLoad(...)**

+ **.GetID()**  "ColID-<DbCollection.ID>"
+ **.GetName()**

+ **.GetAccessPolicy()**   AccessPolicy

+ **.GetColRevisions() []ColRevision**
+ **.GetColRevisionLatest()**:        latest **ColRevisions()** element
+ **.AppendColRevision(...)**

+ **.GetRevStateLatestName()**:       return **ColRevisionLatest().GetRevStateLatestName()**

+ **.GetCreationDate()**             
+ **.GetModDate()**:                  return **ColRevisionLatest().GetModDate**

+ **.IsEditable()**: boolean -     return **ColRevisionLatest().IsEditable()**

  



### ColRevision
+ **ColRevisionNew(...)**
+ **ColRevisionLoad(...)**

+ **GetID()**        "ColRevID-<DbColRevision.ID>" 

+ **GetCollectionID()**  CollectionID   1Collection-to-manyColRevision

+ **GetRevStates() []RevState**
+ **GetRevStateLatest()**:         return latest **RevState**
+ **GetRevStateLatestName()**:     return **GetRevStateLatest().RevStateName**
+ **AppendRevState(newRevState)** 
+ **GetCreationDate()**:           return **GetRevStates()[0].GetCreationDate**
+ **GetModDate()**:                return **GetRevStateLatest().GetCreationDate()**
+ **IsEditable()**:                return **GetRevStateLatest().IsEditable()**


### RevState
```
RevStates flow diagram:

    A ColRev-N starts from the ColRev-N-1 "Ready", and then follows transitions which finally ends-up in either "Ready" or "ErrorZZZZ"
    A ColRev-N+1 can only start from a "Ready"-ColRev-N but cannot start from a "ErrorZZZZ"-ColRev-N

            _________ColRev-N-1_____.___________ ColRev-N _________________________________________________.
                                    .                                                                      .

        CollectionEdit FLOW

                        Ready ___   .                                                               Ready  .
                                 \__.__                                                               A    .
                                    .  V                                                              |    .
                                    . *CollectionEditOngoing  ---> *CollectionEditCancelled  >------->+    .
                                    .       v                                                         |    .
                                    . *CollectionEditCompleted                                        |    .
                                    .       |                                                         |    .
                                    .       v                                                         |    .
                                    . ProvisioningOngoing     --->  ErrorProvisioningFailed           |    .
                                    .       v                                                         A    .
                                    . ProvisioningCompleted   >-------------------------------------->+    .


        NewCollectionCreated FLOW

           *NewCollectionCreated >--.-------------------------------------------------------------> Ready  .

        States prefixed with * are triggered by webpage-user-interactions



```



#### RevStateCollectionEditOngoing
// common fields for all RevStates
- **GetRevStateID()**    "RevStateID-<DbRevState.ID>"
- **GetCreationDate()**
- **GetRevStateName()** 

// specic fields for this RevState
- **UserWhoTriggered()**: **string with user-name**

- **NewRevStateCollectionEditOngoing(...)**
- ? **LoadRevStateCollectionEditOngoing(...)**



#### RevStateCollectionEditCancelled
// common fields for all RevStates
...
// specic fields for this RevState
...

#### RevStateCollectionEditCompleted
// common fields for all RevStates
...
// specic fields for this RevState
...

#### RevStateProvisioningOngoing
// common fields for all RevStates
...
// specic fields for this RevState
...

#### RevStateProvisioningFailed
// common fields for all RevStates
...
// specic fields for this RevState
- **Logs**: binary data (zip/tgz file)

#### RevStateProvisioningCompleted
// common fields for all RevStates
...
// specic fields for this RevState
- **Logs()**: binary data (zip/tgz file)



