## Webpages

```
         (1)
WebHome -->-- WebCollections 
                  |     
                  +-->-- WebCollectionNew  
                  |
                  +-->-- WebCollectionEdit
                  |
                  +-->-- WebCollectionDashboard


(1) user logs.in

```


## WebHome
Public mainpage
- user login
- quick-demo with video or images or explanation
- link to public docs

## WebCollections
Once user logs in
- Table showing all collections belonging to the user
  - One collection per line, with collection name (link'ed to **WebCollectionDashboard**) and resume of last CollectionRevision (modification-date, RevState, date, modified-by user)
  - button "Edit Collection" leading to **WebCollectionEdit** enabled or disabled   
    If CollectionRevision.IsRevStateEditable() == false, then show button disabled

- Button "New collection" (leading to **WebCollectionNew**)

## WebCollectionNew
When user wants to create a new collection
- Ask user:
  - CollectionName
- Button "Create"
  - send message to server, to create the new collection and get back a response with Completed/Failed
    - Response with Completed: should send back and show user collectionID and CollectionName
    - Response with Failed: should send back error and show user error
- Button "Cancel" 
  - send message to server 
  - navigate back to **WebCollections**

## WebCollectionEdit
When user wants to edit a collection
- TODO: Provide detailed instructions for editing a collection
- Button "Save"
  - ...
- Button "Cancel"
  - ...

## WebCollectionDashboard
When user wants to see the details of a collection
- Info from last **CollectionRevision**: last-modification date, user, RevState
- History of previous **CollectionRevision(s)** with dates, user, RevState - in descending order (latest on top)

