# OIDC
```
sample id_token
    {
      "iss": "http://127.0.0.1:5556/dex",
      "sub": "CiQwOGE4Njg0Yi1kYjg4LTRiNzMtOTBhOS0zY2QxNjYxZjU0NjYSBWxvY2Fs",
      "aud": "example-app",
      "exp": 1742254935,
      "iat": 1742168535,
      "at_hash": "KqdHfFeBiAYOLdyJA0Gb5w",
      "c_hash": "217fJAWrwu8-JrVaiBmZVg",
      "email": "admin@example.com",
      "email_verified": true,
      "name": "admin"
    }


```
- claim `sub` is the user's unique identifier, but it's not human-friendly
- claim `name` is the user's human-friendly name

# SPA (reac-app) and webserver (golang webserver)

Golang webserver should:
  + serve public webpages:
    + /                                       WebHome   - only delivers and boots the react-app
    + /login + /callback + /logout                      - oidc login, callback and logout
  + serve /api/private and /api/public endpoints
  + serve /api/private/ws endpoint for websocket communication  

The SPA (react-app) should:
  - handle oauth2 authentication like so:
    + redirect oauth2 paths for the webserver to handle: /login + /callback + /logout
    - spa private pages should use /api/public/check_auth to validate user-access  
      This can be vibe-done with routes added as <PrivateRoute> in App.tsx and then `import { useAuth } from './context/AuthContext';` in the component and some more code.  
      See Collections and Profile as examples

  - use /api/private and /api/public to interact with the webserver
  - use /api/private/ws private websocket


## Spa lingo

- "routes" and "views" and "components" (not "webpages")
- "navigate to a route/view" (not "redirect" nor "load webpage")



# Webpages

```
              (1)
    WebHome \ -->-- WebCollections \collections 
                      |     
                      +-->-- WebCollectionNew \collection\new
                      |
                      +-->-- WebCollectionEdit \collection\edit?collectionID=123
                      |
                      +-->-- WebCollectionDashboard \collection?collectionID=123


(1) user logs.in

```


## Navigation hamburger menu
- Home
- Login
- Logout
- Collections


## WebHome
Public mainpage
+ user login
. quick-demo with video or images or explanation
. link to public docs

## WebCollections
Once user logs in
- Table showing all collections belonging to the user
  - One collection per line, with:
    - collection name (link'ed to **WebCollectionDashboard**)
    - resume of last CollectionRevision: creation-date, modification-date, RevState, last-modified-by user
    - button "Edit Collection" leading to **WebCollectionEdit** enabled or disabled   
      If Collection.IsEditable() == false, then show button disabled
  - opHub.CollectionsListOfUser() returns the list of collections for this user
- Button "New collection" (leading to **WebCollectionNew**)

## WebCollectionNew
When user wants to create a new collection
- Ask user:
  - CollectionName
  - Collection Description
  - AdmissionPolicy: AdmissionPolicyParams
    - AdminUsers
    - AdminGroups
    - ReaderUsers
    - ReaderGroups
- Button "Create"
  - send message to server, to create the new collection and get back a response with Completed/Failed
    - Response with Completed: should send back and show user collectionID and CollectionName. Then redirect to **WebCollectionEdit**
    - Response with Failed: should send back error and show user error. Then redirect to **WebCollections**
  - opHub.CollectionNew() intents to create a new collection
- Button "Cancel" 
  - redirect to **WebCollections**

## WebCollectionEdit
When user wants to edit a collection
- Start the editing of a collection
  - client opens websocket, exchange ws-messages with server to reqquest/reply needed data
  - opHub.CollectionEditOngoing() - at start of editing
  - user edits the form data, which might trigger ws-messages for server to execute blueprint-processes 
  ...
- Button "Save"
  - ...
  - opHub.CollectionEditCompleted()
- Button "Cancel"
  - ...
  - opHub.CollectionEditCancelled())

## WebCollectionDashboard
When user wants to see the details of a collection
- Info from last **CollectionRevision**: last-modification date, user, RevState
- History of previous **CollectionRevision(s)** with dates, user, RevState - in descending order (latest on top)
- query param `collectionID=123` so its easily bookmarkable
- Button "Edit Collection" leading to **WebCollectionEdit**
- Button "Back to Collections" leading to **WebCollections**



